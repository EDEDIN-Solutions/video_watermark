/// Video format for the output (converted) video
///
/// Available formats
/// .mp4,
/// .mkv,
/// .mov,
/// .flv,
/// .avi,
/// .wmv,
/// .gif.
enum OutputFormat { mp4, mkv, mov, flv, avi, wmv, gif }
